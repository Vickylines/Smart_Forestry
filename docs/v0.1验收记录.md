> 历史开发记录。当前公开测试版以 [Beta 1 验收记录](./Beta1验收记录.md) 和项目 README 为准。

**智慧林业 v0.1 验收记录**

日期：2026-09-06。范围：uni-app本机体验版；未部署后台，未执行真实物种识别，未生成签名APK／IPA。

| 检查 | 结果 |
|---|---|
| TypeScript与Vue类型检查 | 通过 |
| 7项领域逻辑测试 | 通过：分组、演示恢复、用户照片无伪造候选、复核冲突、未知状态、CSV及无效输入 |
| Chrome浏览器操作验收 | 通过：建项目、样例批次、复核保存、刷新恢复、筛选、CSV下载、用户照片保存 |
| 浏览器布局检查 | 320px、390px、1100px，无横向溢出 |
| 浏览器运行时异常 | 测试流程未发现 |
| 外部HTTP请求 | 测试流程为零，照片未发往外部服务 |
| H5生产构建 | 通过，dist/build/h5 |
| 微信小程序构建 | 通过，dist/build/mp-weixin |
| Android／iOS共用App资源构建 | 通过，dist/build/app |
| 名称 | 源码界面、标题和manifest已统一为“智慧林业” |

检查结果仅覆盖本次执行的场景。浏览器验收脚本位于apps/client/tests/browser.mjs，领域测试位于apps/client/tests/domain.test.mjs，截图与JSON记录位于.preview目录。

微信开发者工具／真机、Android／iOS真机、相机权限、平台文件配额、原生分享、签名打包和发布尚未验证。DCloud AppID与微信AppID尚未配置。

开发预览：在apps/client目录执行npm run dev:h5后访问http://127.0.0.1:5173/。服务只监听本机。

**界面调整验收（2026-09-06）**

- 六个页面统一了留白、字号、分组、图标与森林绿主操作，说明压缩为短句。
- H5检查六页在320px、390px、1100px下无横向溢出；320px下200%文字允许换行。
- 页面按钮均达到44px触控尺寸；新增键盘创建／取消、禁用控件不会触发的浏览器检查。
- 按下即时反馈，移出后松开不触发；增强对比度、减少透明度和减少动态效果设置已检查。
- 类型检查、原有7项领域测试、浏览器核心流程及三类资源构建均通过。
- 本轮截图与布局报告为.preview/design-*.png及.preview/design-qa.json，流程报告为.preview/browser-qa.json。

本轮没有真机测试结果，资源编译不能代替Android／iOS／微信实际运行验收。
